<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tugasmasjid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".update_tagline"></activity>
        <activity android:name=".update_slideshow" />
        <activity android:name=".update_pengumuman" />
        <activity android:name=".update_marquee" />
        <activity android:name=".update_identitas" />
        <activity android:name=".update_sholat" />
        <activity android:name=".TaglineActivity" />
        <activity android:name=".SlideshowActivity" />
        <activity android:name=".PengumumanActivity" />
        <activity android:name=".MarqueeActivity" />
        <activity android:name=".IdentitasActivity" />
        <activity android:name=".SholatActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>